/** @type {import('next').NextConfig} */
const nextConfig = {
  // Netlify handles Next.js automatically with @netlify/plugin-nextjs
  // No need for standalone output
};

module.exports = nextConfig;

